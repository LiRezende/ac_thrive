<%= form_with(model: class_day, local: true) do |form| %>
  <% if class_day.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(class_day.errors.count, "error") %> prohibited this class_day from being saved:</h2>

      <ul>
      <% class_day.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
