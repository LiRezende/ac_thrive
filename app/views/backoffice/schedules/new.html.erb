<h1>Cadastrar Horário Disponível para Aula</h1>
<p>Selecione abaixo o melhor dia e horário disponíveis para as aulas.</p>
<%= form_with(model: [ :backoffice, @user, @schedule ], local: true) do |form| %>

  <div class="field">
    <%= form.label 'Dia' %>
    <%= form.select(:day, options_for_select(["Segunda-feira", "Terça-feira", "Quarta-feira", "Quinta-feira", "Sexta-feira", "Sábado"]) ) %>
  </div>

  <div class="field">
    <%= form.label 'Horário' %>
    <%= form.time_select :hour %>
  </div>

  <div class="actions">
    <%= form.submit 'Salvar' %>
  </div>
<% end %>