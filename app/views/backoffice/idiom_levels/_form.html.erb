<%= form_with(model: [ :backoffice, @user, @idiom_level ], local: true) do |form| %>
  <% if idiom_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(idiom_level.errors.count, "error") %> prohibited this idiom_level from being saved:</h2>

      <ul>
      <% idiom_level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
